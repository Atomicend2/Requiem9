- [AFK sticker quoted-media download key](afk-sticker-key-fix.md) — downloadMediaMessage must use contextInfo.stanzaId+participant as key, not msg.key (the command message's own key); wrong key causes silent download failure
- [Profile card SVG Unicode fonts](profile-card-fonts.md) — Sharp/librsvg needs noto-fonts + noto-fonts-color-emoji installed; SVG font-family must list Noto first or emoji/Math-Script chars render as codepoints
- [WhatsApp display-name vs mentionTag bug](mention-display-bug.md) — getMentionName() returns pushName (display name); using @${name} in text does NOT create a tappable tag; always use mentionTag(jid) in text + jid in mentions[]
- [getBotSetting missing await pattern](bot-setting-await.md) — getBotSetting is async; calling it without await passes a Promise as sticker/value arg, causing Baileys "Cannot read properties of undefined (reading 'toString')" deep in media prep
- [Web staff role upsert must set _id](web-staff-role-id.md) — web role endpoint must set _id:phone on upsert; without it MongoDB auto-generates ObjectId _id and getStaffList (which reads user_id:d._id) exposes hex ObjectId in .mods output
- [AFK sticker feature key](afk-sticker-design.md) — AFK sticker stored in bot_settings under key afk_sticker:<phone>; eligible: premium/mod/guardian/owner; set by replying .afk to sticker; cleared on return or plain .afk
- [menu_image MongoDB storage](menu-image-storage.md) — .setmenuimg stores buffer in bot_settings key "menu_image" (not local FS); getBotSetting("menu_image") in handleMenu; local FS write is a cache only; FS files are wiped on restart
- [Per-group gambling gate location](gambling-group-gate.md) — gambling_enabled check must be at the TOP of handleGambling() before checkGamblingAccess(); checkGamblingAccess only enforces daily limits, not the group toggle
- [Mazoku card image in bot](mazoku-card-image.md) — getCardImageBuffer() must check mazoku_id BEFORE shoob_id; Mazoku CDN = cdn7.mazoku.cc/cards/<mazoku_id>.webp; without this Mazoku cards always showed SVG placeholder
- [OG tag injection for SPA](og-tag-spa.md) — Express serves one index.html; page-specific OG tags injected in app.ts catch-all by replacing og:title/og:description/og:image strings; cached base HTML; PUBLIC_URL env var sets base URL
- [Staff hierarchy enforcement](staff-hierarchy.md) — addguardian/removeguardian must use isOwnerOrGuardian (not isModOrAbove); mods using isModOrAbove could previously promote/remove guardians above their rank
- [Dungeon+raid reward scale](dungeon-reward-scale.md) — rewards scaled ×10 in July 2026; raid now $50k-$200k base; dungeon floor extra now $8k/floor (was $800); getDungeonEnemy reward array updated
- [Gambling streak system](gambling-streak.md) — gambling_streak field on users; streakWin(streak, prob) applies up to +8% win boost after 3+ losses; gambleUpdate(limit, extra, won) tracks streak direction; slots/horse not adjusted (inherent variance)
- [Card loader memory optimization](card-loader-memory.md) — loaders check file-size metadata in sync_meta collection BEFORE reading JSON; avoids 50k-card JSON loads on cold starts when DB already has the data
- [MongoDB indexes for cards](mongo-cards-indexes.md) — text index on cards.name+series for fast search; sparse unique on shoob_id and mazoku_id to prevent duplicates; all registered in ensureIndexes() in mongo.ts
- [from-mazoku endpoint uses MongoDB](from-mazoku-endpoint.md) — uses MongoDB (source:"mazoku") not the JSON file; reading mazoku_cards.json on every HTTP request was causing repeated 14k-card memory spikes
- [Welcome/leave auto-enable](welcome-leave.md) — setwelcome/setleave must set welcome/leave:"on" atomically; separate steps mean users forget the toggle.
- [WhatsApp mention JID requirement](mention-jid.md) — mentionTag() text alone is not enough; the JID must also be in the mentions array or WhatsApp shows gray non-tappable text.
- [GIF cards need ffmpeg conversion](gif-card-convert.md) — WhatsApp rejects GIF natively; only WebM URLs stream directly; GIF URLs must be downloaded and converted to MP4 via ffmpeg.
- [Profile card compositing order](profile-card-frame.md) — frame must be composited BEFORE avatar so it shows as an outer border ring, not covering the avatar face.
- [MongoDB migration complete](mongo-migration.md) — Full SQLite→MongoDB migration done; col() helper used everywhere; getDb() is a never-stub; only MONGODB_URI credentials block startup.
- [Bot build & workflow](bot-build-workflow.md) — esbuild via build.mjs (not tsc); run `node build.mjs` in api-server to build; workflow uses `cd artifacts/api-server && pnpm run start` (NOT pnpm run dev — that triggers shadow-garden vite build which fails).
- [queries.ts getRpg duplicate risk](bot-build-workflow.md) — getRpg already exists at ~line 712 using col("rpg_characters"); appending a second export causes build error; check before adding new functions.
- [Bot RPG & gambling patterns](bot-rpg-design.md) — STR/AGI/INT/LCK attribute system layered on top of base stats; Baileys real-time animation via sock.sendMessage(jid, {text, edit:key}); mongodb must be in build.mjs external array.
- [getActiveMute missing await](mute-await-bug.md) — getActiveMute is async; calling it without await makes the condition always truthy (deletes every message). Always await async queries used as if-conditions.
- [.ci card OOM pattern](card-ci-oom.md) — .ci loops over all matches and downloads one video buffer per match; cap at 3 results + add 20 MB buffer size guard in getCardImageBuffer to prevent status 134 OOM crashes.
- [Lottery ticket buy flow](lottery-buy-flow.md) — items with effect "lottery_entry" must directly increment users.lottery_tickets on buy, not go to inventory; bank_cap: items also consumed on buy not deferred to .use.
- [Card search targeted queries](card-search-targeted.md) — searchCardsByName/searchCardsBySeries added to queries.ts; use escapeRegex() helper (already imported); Edit tool corrupts regex strings with backslash — use Python shell injection instead for functions containing regex literals.
- [Lottery web-bot ObjectId sync](lottery-objectid-sync.md) — lottery_entries.lottery_id is stored as ObjectId by bot; routes/v1/lottery.ts must query with activeLottery._id (ObjectId), not lotteryId.toString() — string query always returns 0 entries.
- [queries.ts Edit tool hazard](queries-edit-hazard.md) — Edit tool pattern-matches on old_string including backslash/regex chars; inserting functions with regex literals via Edit corrupts the file. Use Python shell script to inject at a line number instead.
- [withMediaSlot missing from connection.ts](media-slot-export.md) — withMediaSlot semaphore (max 2 concurrent animated sends) lives in connection.ts; cardspawn.ts imports it; must be exported or build fails with "No matching export" error.
- [Auction bid balance lookup bug](auction-bid-bug.md) — auctions route was re-fetching user by phone/wa_sender (often misses); use req.user.balance from middleware directly since requireAuth already fetches the full user doc.
- [.ll/.lp pollResult invalid](lottery-poll-result.md) — pollResult is NOT a valid outgoing Baileys message type; use plain sendText with progress-bar format instead.
- [JID normalization for @mentions](jid-normalization.md) — bare phone numbers ("1234567890") in mentions array are not valid JIDs; must call normalizeId() to get "1234567890@s.whatsapp.net" before passing to mentions[]; affects .active/.inactive and .ci owners.
- [Sticker mimetype conflict](sticker-mimetype.md) — passing mimetype:"image/webp" in sock.sendMessage sticker calls conflicts with Baileys internal type handling in some versions; always send { sticker: buf } without explicit mimetype.
- [yt-dlp/ffmpeg on Replit](ytdlp-ffmpeg-replit.md) — must install via installSystemDependencies (Nix packages "ffmpeg","yt-dlp"); start.sh curl fallback unreliable; system PATH install is authoritative.
- [.join groupAcceptInviteV4 wrong JID](join-v4-bug.md) — groupAcceptInviteV4(from, ...) must receive the INVITER's JID not the current group JID; using group JID caused account_reachout_restricted error; use groupAcceptInvite(code) for raw invite codes.
- [PvP duel system](pvp-system.md) — .duel @user challenges; accept/decline routes PvP-first via pendingPvpChallenges.has(ctx.sender) check in message.ts; both parties get SP/XP/gold; 30min cooldown
- [Mentorship system](mentorship-system.md) — .mentor @user offers; .mentor accept/leave; mentor earns +1SP+50XP per apprentice quest, +1SP+80XP per dungeon; apprentice gets +15% XP bonus; max 3 apprentices; mentor must be Lv8+ and 5 levels above
- [Daily/weekly quest boards](quest-boards.md) — .quests (daily board) and .weekly (weekly board); claim with .quests claim / .weekly claim; progress tracked via daily_quests + weekly_quests collections; mentor_sessions weekly quest feeds from mentorship activity
- [PvP duel system](pvp-system.md) — .duel @user challenges; accept/decline PvP-first routing via pendingPvpChallenges check in message.ts; 30min cooldown; Win: +3SP+$2k, Lose: +1SP+$500
- [Mentorship system](mentorship-system.md) — .mentor @user offer; Lv8+ mentor, 5-level gap required; apprentice +15% XP on quests; mentor +1SP+XP per apprentice quest/dungeon; max 3 apprentices
- [Daily/weekly quest boards](quest-boards.md) — .quests (daily) and .weekly board; .quests claim / .weekly claim; progress auto-tracked from quest/dungeon/raid/pvp/mentor hooks
